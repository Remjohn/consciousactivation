# Phase 7 — Conscious Activations Studio

Phase 7 turns the Phase 1 shell into a development-only supervisory application surface.

The reference flow is:

```text
campaign order
→ Harness-to-Studio routing
→ source-to-batch Control Tower projection
→ canonical timeline projection
→ operator natural-language or direct-manipulation correction
→ typed ChangeRequestProgram
→ descendant-only selective rerun request
→ HumanResolutionEpisode
→ programming-material indexing
→ explicit ship decision
→ source-to-ship audit export
```

Start with `docs/implementation/PHASE_07_IMPLEMENTATION_REPORT.md` and the audited Specs `TS-CAS-001` through `TS-CAS-006`.

This phase does not activate Format 02, VAE Stage 5, external providers, production publication, or certification.
