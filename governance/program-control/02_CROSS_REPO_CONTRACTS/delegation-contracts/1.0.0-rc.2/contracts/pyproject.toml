[project]
name = "cmf-delegation-contracts"
version = "1.0.0rc2"
requires-python = ">=3.11"
readme = "README.md"

[tool.setuptools]
package-dir = {"" = "generated/python"}

[tool.setuptools.packages.find]
where = ["generated/python"]
include = ["cmf_delegation_contracts*"]
