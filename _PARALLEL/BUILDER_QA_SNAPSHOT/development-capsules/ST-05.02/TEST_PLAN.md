# Test Plan

Implementation begins only after the exact authorization phrase is supplied. Require the then-current complete repository regression, expected initially at 380/380 PASS with no mandatory skips.

Story tests:

- consume the exact active ST-05.01 snapshot and receipt;
- assess all five capability requirements exactly once;
- prove no target failure and exact deterministic Builder-code ownership;
- verify the full governed alternative order and selected owner for each capability;
- emit `NO_NEW_SKILL_REQUIRED` with zero skills, adaptations, recipes, packages, or capsules;
- emit `NOT_APPLICABLE_NO_GAP` without fabricating a Skill Design Brief;
- reject a gap or material adaptation without complete evidence, typed brief, and human authority;
- reject an unnecessary skill where code, schema, validator, workflow, reference, human decision, reuse, adaptation, or adapter is adequate;
- reject altered, stale, superseded, invalidated, or wrong-version snapshot/context lineage;
- reject unauthorized actors and every prohibited skill operation;
- prove canonical ordering, fresh-context equality, and decision/receipt hash reproducibility;
- prove payload-safe idempotency and conflicting-command rejection;
- prove replay, resume, upstream invalidation, and historical reproduction;
- inject atomic failure, prove zero partial state, and retry successfully;
- verify observations, architecture boundaries, and exact file scope;
- run every predecessor regression.

No network, provider, database, GPU, evaluator, VAE, Delegation runtime, Format 02, conversational, or production fixture is permitted.
