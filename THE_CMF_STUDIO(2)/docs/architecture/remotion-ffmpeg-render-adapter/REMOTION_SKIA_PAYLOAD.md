# Remotion + Skia Payload

The Remotion job preserves:

```text
timeline_program_id
composition_id
input_props_ref
CompleteEditingSessionRenderStateWrapper
SevenLayerCompositionPayload
motion vocabulary receipt
memetic sound cue receipt
```

The job prepares a command plan; it does not execute unless explicitly gated.
