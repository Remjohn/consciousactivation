# Test Plan

Tests verify:

```text
Format 01 packet maps to Format 01 extraction packet ref.
Format 02 packet maps to Format 02 extraction packet ref.
Format 03 packet maps to Format 03 extraction packet ref.
Format 04 packet maps to Format 04 extraction packet ref.
SuperVisual packet maps to SuperVisual format program.
Carousel packet maps to Carousel format program.
Meme, Poll, Reaction packets map correctly.
Batch compilation can authorize programs and emit engine adapter payloads.
Bridge fails if source refs are missing.
```
