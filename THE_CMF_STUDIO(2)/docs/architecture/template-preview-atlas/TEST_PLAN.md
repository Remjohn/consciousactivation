# Test Plan

Tests verify:

```text
slot maps require slots
sample payloads satisfy required slots
preview requests cannot execute providers/renderers
SuperVisual preview compiles visual SVG
Carousel preview requires continuous slide indexes
Format 02 preview compiles avatar/object/proxy slots
template approval cannot pass with blockers
template versions require hash
template atlas can register previews and versions
generic preview service routes correctly
```
