# Carousel Template Previews

Carousel templates are sequence-aware.

V1 preview rules:

```text
slides must have continuous indexes
each slide needs a role
each slide should have visible slot labels
closure slide should be represented
```

The preview SVG renders a small strip of slide cards.
