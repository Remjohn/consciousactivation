# Carousel Format Adapter

The adapter converts a `CarouselFormatProgram` into a carousel-engine-ready input object.

It keeps:

```text
carousel thesis
sequence steps
closure contract
viewer state sequence
composition grammar
layer stack
style route policy
eval gates
source refs
```

It does not rebuild slide copy or render slides.
