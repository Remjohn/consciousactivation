# Test Plan

Tests verify:

```text
hybrid design disables lip sync
face plates reject phoneme/viseme behavior
performance plans reject lip_sync_enabled=True
64-state library compiles 8×8 states
all 4 audience proxy personas exist
audience proxies require SFL functions
prop attachments require sockets and prop refs
uncanny risk fails face morphing/lip-sync indicators
Format 02 scene adapter emits AvatarPerformancePlan
render payload is not final render
```
