# Tool Stack Notes

V1 is runtime-agnostic but plans for:

```text
Stretchy Studio = authoring / layer rig / mesh deformation / clip creation
Spine Runtime = professional 2D skeletal runtime, subject to licensing
DragonBones = alternate JS runtime
Remotion = final video composition wrapper
Motion Canvas = diagram/vector motion companion
```

The bundle does not call any of these tools yet.
