# SuperVisual and Carousel Format Programs

## SuperVisual

Requires one source truth, one visual hook, one edge product, a composition role, and a negative space policy.

## Carousel

Requires thesis, slide sequence grammar, viewer-state sequence, source-backed claims, open loop, and closure contract.

Format Intelligence does not render either asset. It compiles the production grammar that the component engine consumes.
