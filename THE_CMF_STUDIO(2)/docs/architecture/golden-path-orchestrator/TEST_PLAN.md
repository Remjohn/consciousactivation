# Test Plan

Core test:

```text
test_health_myth_format02_golden_path_runs_source_to_fake_export
```

Additional gates:

```text
brand_context_version_id required
source refs preserved
8 scenes required
composition decisions locked
avatar plans no-lip-sync
proxy plans have SFL function
video timeline compiles
fake proxy/final renders emit hashes
export requires approval
```
