# Current Project Status — Delegation Protocol

Updated: 2026-07-15

- Current authority: Delegation PRD V1.1 and Activative Intelligence
  Constitution V1.1.
- RC1 remains immutable with status `consumer_rejected`.
- RC2 remains immutable with status `convergence_rejected`.
- RC3 remains immutable with status `convergence_rejected`; its receipt,
  manifest, hashes, and clean-room evidence are preserved unchanged.
- RC4 is the current local unsigned release candidate. It adds only portable
  derivative wrong-reading-lock inheritance contracts, structured evidence,
  generated types, fixtures, validator/migration/compatibility enforcement,
  reference-engine validation, and directly required documentation.
- Exact inheritance and stricter additions pass. Removal, weakening, missing
  parent evidence, ambiguous derivation, unauthorized semantic relaxation, and
  parse-without-enforcement fail with typed outcomes.
- Visual Asset Demand and VAE boundary bytes remain unchanged. Envelope
  protocol remains `1.0`; Visual Asset Demand remains `1.1`; derivative lock
  inheritance is message `1.0`; package candidate is `1.1.0-rc.4`.
- Source and clean-room suites pass: 83 validator tests, 35 protocol tests,
  27 examples, 47 fixture JSON files, 4 migration declarations, zero stale
  active package declarations, and zero transient files.
- The local and Program Control RC4 trees contain 164 identical files.
- Program-level issue identities are governed only by
  `../CMF_PROGRAM_CONTROL/04_CROSS_REPO_ISSUES/XRI_REGISTRY.yaml`.
  Delegation records exact XRI references but does not define their titles,
  owners, severities, states or resolution criteria.
- The unrelated Delegation evidence blockers formerly mislabeled with those
  identifiers are now `DLG-ISSUE-001` and `DLG-ISSUE-002`; both remain open.
- The former local workflow-documentation condition `XRI-018` is now the
  closed local record `DLG-ISSUE-018`; its historical identifier remains
  traceable in Program Control and the Delegation issue register.
- Current program references are `XRI-001` through `XRI-017` and `XRI-019`
  through `XRI-021`. Their canonical statuses must be read from the Program
  Control registry.
- Production authorization remains `false`. External owner ratification,
  product adoption, trust roots, signing, Control Tower integration,
  transport/state ownership, and operational certification remain open.
