# Shared packages

- `ca_contracts` — generated bindings, canonical serialization, and strict contract validation.
- `ca_runtime` — local SQLite development runtime and product shell helpers.

These packages are Phase 1 development infrastructure, not independent product authority.
