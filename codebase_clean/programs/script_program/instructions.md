# Script Program Execution Instructions

## Operational Workflow
1. **Admit Context**: Hunter admits verified SemanticProgram and Evidence context via `request_jit_authoring`.
2. **Propose Script**: Composer structures scenes and turns adhering to Voice DNA via `propose_script`.
3. **Evaluate Semantic QA**: Analyst checks forbidden centroids, SFL register, and quote fidelity via `evaluate_semantic_qa`.
4. **Compile Final Package**: Composer compiles immutable segment sequence with SHA-256 via `compile_final_script`.
5. **Operator Approval Gate**: Commander presents exact script bytes to human operator for authoritative sign-off via `approve_script`.
6. **Activation Transfer Contract**: Commander generates downstream contract for CompositionIR / VAE via `create_transfer_contract`.
