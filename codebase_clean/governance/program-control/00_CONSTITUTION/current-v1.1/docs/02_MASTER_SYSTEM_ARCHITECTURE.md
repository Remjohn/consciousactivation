# Master System Architecture

**Version:** 1.1.0  
**Status:** Constitutional architecture map

## 1. Constitutional product topology

```text
┌──────────────────────────────────────────────────────────────┐
│ Activative Intelligence & Visual Narrative Constitution      │
│ Highest-order semantic, visual, role, and evaluation law     │
└──────────────────────────────┬───────────────────────────────┘
                               ↓
┌──────────────────────────────────────────────────────────────┐
│ CMF Atomic Harness Builder                                   │
│ Compiles category constitutions, Harness IR, workflows,      │
│ contracts, skills, benchmarks, and Development Capsules      │
└───────────────┬───────────────────────────────┬──────────────┘
                │                               │
                ↓                               ↓
┌──────────────────────────────┐  ┌────────────────────────────┐
│ Conversational Activation    │  │ Visual Format Categories   │
│ / Human Expression           │  │ Video / Character /        │
│ Comment, Reply, ReelCast,     │  │ Carousel / Supervisual     │
│ Interview Expression         │  │                            │
└───────────────┬──────────────┘  └──────────────┬─────────────┘
                │                                │
                └───────────────┬────────────────┘
                                ↓
                    Atomic Content Harnesses
                                ↓
┌──────────────────────────────────────────────────────────────┐
│ Content Harness ↔ Visual Asset Editor Delegation Protocol    │
│ Preserves semantic authority, versions, lifecycle, contracts │
└──────────────────────────────┬───────────────────────────────┘
                               ↓
┌──────────────────────────────────────────────────────────────┐
│ Visual Asset Editor                                          │
│ Resolves assets through retrieval, editing, generation,      │
│ ComfyUI, LoRAs, candidate evaluation, repair, and delivery   │
└──────────────────────────────┬───────────────────────────────┘
                               ↓
┌──────────────────────────────────────────────────────────────┐
│ Visual Syntax / Composition Runtime                          │
│ Exact BBOX + WHY, hierarchy, attention path, rendering       │
└──────────────────────────────┬───────────────────────────────┘
                               ↓
                    Final asset + evaluation + memory
```

## 2. Shared semantic compilation

```text
Research evidence
→ Coach / Guest Identity DNA
→ Audience Context Premise
→ Coach/Guest–Audience Resonance Map
→ Matrix of Edging
→ Activation Compiler Harness
→ Activative Intelligence Pack
```

The pack is the semantic authority. A downstream system may receive sparse compiled tokens, but every token remains traceable to the frozen rich pack.

## 3. Conversational branch

```text
Activative Intelligence Pack
→ Activative Call sequence
→ human reply or interview answer
→ Reaction Receipt
→ Expression Moment
→ post-expression primitive / coalition / Edge Product pass
→ final Content Type Contract
```

### Interview Expression specialization

```text
Guest Context Workspace
→ guest and audience research
→ Interviewer Resonance Context
→ Activative Intelligence Packs
→ Interview Brief and Interview Asset Contracts
→ human-led Complete Expression Session
→ final transcript
→ Expression Moment Inventory
→ Identity DNA amendment proposals
→ distributed Content Type Contracts
```

Pre-session format routes are portfolio hypotheses. The transcript authorizes final contracts.

## 4. Visual branch

```text
Activative Intelligence Pack
→ Visual Semantic Pack
→ Semiotic MCDA
→ Visual Narrative Program
→ category and format harness
→ high-leverage Feature Contracts
→ Composition Asset Pack
→ Delegation Visual Asset Demand
→ Visual Asset Editor
→ image-conditioned geometry
→ Visual Syntax / composition
→ final render
```

### Semantic ownership

- Activative Intelligence chooses pressure, role, stance, identity urge, and desired reaction.
- Visual Semantics chooses recognition intent and carrier.
- Visual Narrative chooses attention-state architecture, pattern entry, interruption, prediction, payoff, affinity, and residue.
- Format harnesses own surface mechanics.
- Visual Syntax owns exact composition.
- The VAE owns asset-resolution method and production.

## 5. Dual-order law

```text
Harness-development evidence order:
normalize specimens → parse syntax → classify variables → infer function → derive grammar

Runtime production order:
compile activation → choose semantic carrier → design narrative → compose → produce → evaluate
```

## 6. Knowledge and memory architecture

The query substrate may use relational, graph, vector, and media indexes. CMF-OKF is the portable human/agent-readable projection, not transactional truth.

```text
current authority and task
→ deterministic filters
→ typed graph expansion
→ lexical / semantic / visual retrieval
→ VLM reranking
→ contradiction coverage
→ Minimum Complete Context
→ JIT Execution Capsule
```

## 7. Learning loops

### Prospect and conversational loop

```text
Activative Call → reaction → recompilation → continue / elevate / close
```

### Interview loop

```text
Interview Brief → answer evidence → Expression Moment → final route → Identity DNA proposal
```

### Visual loop

```text
semantic candidates → Semiotic MCDA → narrative program → render → constitutional eval → repair → golden/failure memory
```

### Brand loop

```text
interaction evidence → governed Identity DNA revision → resonance revision → future activation quality increase
```

## 8. Versioning laws

Every production chain freezes:

- Identity DNA version;
- Context Premise snapshot;
- Resonance Map version;
- Matrix of Edging result;
- Activative Intelligence Pack version;
- Expression Moment or source-evidence versions;
- format and category constitution versions;
- visual semantic and narrative program versions;
- feature contracts;
- Visual Syntax and T/V versions;
- evaluator and compiler versions.
