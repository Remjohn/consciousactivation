[project]
name = "cmf-delegation-validators"
version = "1.0.0rc2"
requires-python = ">=3.11"
dependencies = ["jsonschema>=4.0", "pyyaml>=6.0"]

[tool.setuptools.packages.find]
where = ["."]
include = ["cmf_delegation_validators*"]
