[project]
name = "cmf-delegation-protocol-reference"
version = "0.1.0.dev1"
requires-python = ">=3.11"
dependencies = ["cryptography>=42.0", "jsonschema>=4.0"]

[tool.setuptools.packages.find]
where = ["."]
include = ["cmf_delegation_protocol*"]

[tool.pytest.ini_options]
testpaths = ["tests"]

