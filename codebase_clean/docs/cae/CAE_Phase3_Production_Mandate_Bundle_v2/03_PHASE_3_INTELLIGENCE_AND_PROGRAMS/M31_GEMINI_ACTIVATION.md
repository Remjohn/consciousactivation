# GEMINI EXECUTION — M31 — Knowledge Clusters + Research Signals + Context Projection

Execute ONLY CAE M31.

Before action:
1. Read the complete baseline authority set in `00_CONTROL/01_BASELINE_AUTHORITY_READ_SET.md`.
2. Read every mandate-specific reference below in full.
3. Report the exact files read and the current repository commit.
4. Inspect live symbols/callers/tests before editing.

Objective:
Separate knowledge organization from temporal/contextual signal detection: Knowledge→Cluster→Signal→Context Projection. Produce typed outputs for downstream ontology/state reasoning.

Mandatory references:
M29/M30; Editorial dependency graph; research architecture; current retrieval/status.

Non-negotiable CAE constraints:
- CAE authority, Workspace scope, state contracts, typed operations and receipts remain canonical.
- Hunter / Analyst / Composer / Commander remain distinct.
- Canonical Skills remain passive, versioned and flat; no Skill-to-Skill invocation.
- Protected source-bearing Guest/Audience/research evidence cannot be silently rewritten. Derived expressions may be versioned/rebuilt only with lineage.
- OKF is a curated knowledge representation; Supabase/Postgres remains operational authority. Do not introduce Redis as canonical knowledge/state.
- Do not rebuild already-verified Interview Intelligence/Composer components.
- Synthetic/placeholder material may be used for tests but can never satisfy a production claim.

Execution:
- Reconcile the live repository against the mandate.
- Reuse existing services/operations/schemas.
- Make the smallest justified implementation.
- For state changes prove source state → operation → target state → receipt → recovery.
- For agent reasoning prove lane → Agent/Team → Skills/tools → structured output → provenance.
- For hooks/validators prove event → guarantee → allow/block → evidence.

Acceptance target:
Real research produces typed clusters and time/context-aware ResearchSignals consumable by downstream state/hypothesis logic; signals are not treated as canonical truth.

Evidence required:
- commands, environment, fixture/data class, test results;
- runtime trace where applicable;
- artifact/receipt identifiers;
- contrastive/negative test;
- limitations and unresolved blockers;
- exact commit SHA.

STOP if authority conflicts, protected-source mutation, lane collapse, Skill nesting, a new canonical object becomes necessary, or completion cannot be proven.

Do not begin another mandate. After the evidence report and operator decision request, STOP.
