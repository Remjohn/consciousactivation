# Phase 7 Gate — Workflow Engineering

Required evidence:
- primitive constitution;
- executable Workflow IR;
- deterministic control-flow compiler;
- Step Contracts on representative Programs;
- loop termination, branch, join and timeout tests;
- checked transition-order proof (edge → checks → persistence/hooks → guards → commit → target entry refresh);
- checked transition-order proof (edge → checks → persistence/hooks → guards → commit → target entry refresh);
- no second workflow engine introduced;
- CURRENT.md synchronization.
