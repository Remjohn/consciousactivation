# M11 — End-to-End Reality-Contact Regression

**Status:** PROPOSED
**Depends on:** M01–M10
**Primary requirements:** all FR-IP requirements

## Objective

Prove the integrated Interview Program against real repository paths and staging/runtime infrastructure, not only mocked unit contracts.

## Required proof chain

`real upstream candidate source`
`→ Operator selection`
`→ real Question Program resolution`
`→ real existing Activative Interview Brief`
`→ real Composer Session / approved runtime seam`
`→ real answer/input`
`→ observation`
`→ evidence lineage`
`→ content candidate readiness`
`→ Operator review.

## Required adversarial attacks

- receipt without reality;
- schema-only success;
- wrong-workspace reference laundering;
- generic answer passing structural validation;
- archetype laundering;
- score/performance proxy replacing semantic evidence;
- question-count gaming;
- Operator bypass;
- stale UI overwriting current state;
- lock enforced only in UI;
- rejected candidate leaking into launch.

## Required human check

Review at least one accepted and one rejected candidate and record why the accepted one is meaningfully grounded/useful and why the rejected one is not.

## Documentation closure

Before marking M11 complete:

- update the relevant PRD section if durable product truth changed;
- update the relevant tech spec section if implementation deviates from the spec;
- record verified/unverified/open claims;
- refresh any gap/ledger entry required by repository convention;
- update this program's execution evidence.

## Completion rule

A green automated suite without runtime/human evidence is **NOT COMPLETE**.
