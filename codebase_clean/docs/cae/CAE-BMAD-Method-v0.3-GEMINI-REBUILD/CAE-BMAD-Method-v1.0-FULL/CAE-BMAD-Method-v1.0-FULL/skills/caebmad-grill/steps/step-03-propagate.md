# Grill Step 03 — Propagate Decision

After the operator answers:

- record the chosen decision;
- record the rejected alternatives;
- identify downstream artifacts;
- identify affected source interpretations;
- identify implementation consequences;
- identify whether any previous proposed text must be rewritten;
- update the contradiction register.

Do not silently rewrite unrelated documents.
