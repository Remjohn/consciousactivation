# Threshold Calibration Report

Classification: `non_production_readiness_proof`  
Profile: `character-scene-composition@1.1.0-draft`  
Calibration status: **not executed**  
Final thresholds defined: **false**

## Available inputs

- The evaluation program `character-scene-evaluator@1.1.0-proof` and its prompt template are SHA-256 pinned.
- The 12-case calibration seed is present but provisionally labeled and not authority-adjudicated.
- Six protected-set case slots are disjoint by identifier, but the set is unsealed, labels are withheld and content materialization remains pending.
- Non-compensable gates and error-reporting semantics are proposals, not approved release rules.

## Missing inputs

No evaluator provider/model or weight digest, runtime/API version, independent credential identity, evaluator score distribution, uncertainty distribution, adjudicated ground truth, executed false-positive/false-negative analysis, sealed protected-set run, arbitration result or rollback rehearsal exists.

Consequently no operating point, ranking threshold, hard-gate numeric boundary, error ceiling or certification claim is selected. The evaluator remains `specified_not_certified` with evidence status `insufficient_evidence`.
