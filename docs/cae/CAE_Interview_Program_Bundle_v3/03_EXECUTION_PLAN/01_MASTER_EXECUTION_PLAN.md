# Interview Program Master Execution Plan

**Status:** READY FOR OPERATOR REVIEW — not execution-authoritative until PRD/spec ratification
**Build authority:** false

## 1. Execution law

The plan uses the repository's brownfield sequence:

`Reconnaissance → Plan → Execute → Verify → Report`

Every mandate is one bounded execution unit. Do not batch unrelated mandates into one Gemini run. Each mandate must be read against the current repository before editing, and implementation completion is not claimed until actual tests/evidence are recorded.

## 2. Phase 0 — Authoring/ratification (no code)

1. Accept/revise `00_GOVERNANCE/03_PRD_DELTA.md` against `docs/PRD/CURRENT.md`.
2. Accept/revise `01_SYNTHESIS/01_QUESTION_INTELLIGENCE_SYNTHESIS.md`.
3. Reconcile the 12-dimension coordinate model against live CAE/AIR ownership.
4. Accept/revise the Interview Program technical specs.
5. Freeze the implementation matrix.

**Gate:** Build authority may remain false until all five are operator-accepted.

## 3. Phase 1 — Brownfield reconciliation

### M01
Prove actual current repository boundaries, symbols, routes, repositories, and test patterns.

**Must produce:** exact file/source inventory; gap list; ownership map; no-new-object decisions; proposed execution edits.

## 4. Phase 2 — Upstream hypothesis integration

### M02
Build the adapter/view that consumes existing activation-hypothesis/portfolio authority and supports the derived candidate coordinate model.

### M03
Implement the Question Intelligence resolver using the accepted synthesis. No primitive canonization occurs here unless a separate promotion decision exists.

## 5. Phase 3 — Existing Brief integration

### M04
Compile selected hypothesis/question material into the existing Activative Interview Brief using the existing Composer contract.

**Gate:** Brief compilation must pass before runtime adaptation work proceeds.

## 6. Phase 4 — Operator control surface

### M05
Implement/extend the Operator Studio against the current application shell, API, persistence, auth, and Composer boundary.

**Gate:** Operator can inspect provenance, select/reject/edit/regenerate/defer/lock, and approve without client-side bypass.

## 7. Phase 5 — Runtime intelligence

### M06
Implement bounded adaptive frontier logic: deterministic spine + 3–5 eligible candidates when sufficient valid alternatives exist.

### M07
Implement answer observation/state transition behavior and the evidence/inference distinction.

These two may be parallelized only after M04's live Brief contract is stable.

## 8. Phase 6 — Downstream semantic compatibility

### M08
Implement/verify Question-to-Composition compatibility views/logic for archetype, format, narrative role, and expected response shape.

### M09
Implement/verify authenticated evidence lineage/handoff and content candidate readiness without forcing unsupported downstream outputs.

## 9. Phase 7 — Outcome and end-to-end proof

### M10
Implement content-menu readiness and Operator review trace.

### M11
Run complete staging/reality-contact regression, including adversarial false-proof tests and documentation maintenance.

## 10. Dependency graph

`M01 → M02 → M03 → M04 → M05 → {M06, M07} → M08 → M09 → M10 → M11`

M06 and M07 may run in parallel after M04 if both consume only stable contracts.

## 11. Completion definition

The program is not complete when code merges. It is complete only when:

- requirements are ratified;
- each mandate has an execution receipt;
- tests run against real repository paths;
- a real staging trace demonstrates hypothesis → Brief → interview input → observation → evidence lineage;
- Operator Studio actions persist and enforce authorization;
- false-proof attacks fail;
- PRD/spec maintenance is complete in the same session as durable implementation changes;
- open gaps are explicitly carried forward.
