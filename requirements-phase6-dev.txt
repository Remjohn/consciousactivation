# Phase 6 uses the repository's existing Python development environment.
# Required Python test/runtime packages are intentionally minimal.
pytest>=8
# External command required for actual media proof:
# ffmpeg + ffprobe 7.x or a separately validated compatible version
# Optional real static runtime:
# skia-python (not required for the explicitly labeled development fallback)
