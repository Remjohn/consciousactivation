# Conscious Activations — Full Next Campaign Prompts V3
# Parallel Execution Map

## Mandatory sequential control path

```text
Prompt 1 — Authority convergence
    ↓
Prompt 2 — Canonical reconciliation
    ↓
Prompt 3 — Spec writing factory
    ↓
Prompt 4 — Independent audit factory
    ↓
Prompt 5 — Revision factory
    ↓
Prompt 6 — Independent re-audit and acceptance
    ↓
Prompt 7 — Development Capsules and build-readiness
    ↓
Prompt 8 — Shared contracts
```

These stages must remain sequential because each freezes authority or shared bytes consumed by the next stage.

## Parallel work inside Prompt 3

After the Extra High controller freezes lane packets, run these High writers simultaneously:

- Lane A — shared authority and contracts
- Lane B — Activative Intelligence Runtime
- Lane C — Atomic Harness Pipeline kernel and retrieval
- Lane D — Interview Expression and source packages
- Lane E — video, static, and animation runtimes
- Lane F — Studio, revisions, and HumanResolution
- Lane G — evaluation, repair, retrieval, and Programmed Models
- Lane H — VAE, Delegation, workers, operations, and release boundaries

Each agent writes **one Tech Spec per execution**. Multiple agents may run at once only on disjoint canonical files.

## Parallel work inside Prompt 4

Every written spec may be audited in parallel by a different High agent. The writer may not audit its own spec. Shared-schema conflicts go to the Extra High controller.

## Parallel work inside Prompt 5

Independent revisions may run in parallel. Any revision touching shared schemas, object ownership, compatibility policy, or two products waits for an Extra High architecture decision.

## Parallel work inside Prompt 6

Independent re-audits may run in parallel. Final cross-spec convergence and accepted hash locking are performed sequentially by one Extra High integrator.

## Parallel work after Prompt 8

```text
AIR build waves ──────────┐
                          ├→ Prompt 10 → Prompt 11
AHP build waves ──────────┘
```

AIR and AHP may build in parallel only after the shared contract release is frozen, their paths are disjoint, and every child build targets one accepted spec.

## Sidecars that may start during Prompts 1–7

These may run concurrently on isolated documentation or fixture paths:

- operator interview/source preparation;
- CMF Studio archive inventory and source-reuse verification;
- local FFmpeg/Node/Remotion/HyperFrames/Skia readiness inventory;
- VAE external-readiness documentation, without Stage 5;
- external dependency/version/license review;
- clean-environment and test-fixture preparation.

They may not change current authority, canonical schemas, product source, or accepted specs.


---

# PROMPT 01 — Program Control V2.1 Authority and Status Convergence

**Model:** GPT-5.6 Sol Extra High  
**Parallel:** No. This is the canonical control-path gate.

Prepare the Program Control V2.1 Authority Convergence and Repository Status-Truth Reconciliation Package.

Do not implement product source code. Do not create AIR or AHP source trees. Do not begin VAE Stage 5. Do not modify Builder behavior. Do not rewrite historical receipts, immutable releases, rejection records, or historical status snapshots.

The purpose is to establish one current authority chain, one truthful repository state, and one product-ownership model before specification or implementation work.

## Mandatory inputs

Byte-open and hash-verify:

- `CONSCIOUS_ACTIVATIONS_AHP_PRD_V1_2_PRIMITIVE_ARCHETYPE_CENTERED.zip`
- `CONSCIOUS_ACTIVATIONS_ACTIVATIVE_INTELLIGENCE_RUNTIME_V2_1_FULL_BUNDLE.zip`
- `THE_CMF_STUDIO(2).zip`
- the updated Specs Builder Library containing the CA lifecycle Skills

Read every applicable root/product `AGENTS.md`, Program Control authority/status registry, Builder terminal summary/model-neutral handoff/evidence records, VAE status/readiness records, Delegation release/status records, and cross-repository issue registry.

If a mandatory input cannot be opened and byte-verified, stop with `V2_1_MISSING_INPUT_REPORT.yaml`. Do not reconstruct it from summaries.

## Work

1. Create a source-input lock with path, bytes, SHA-256, archive member count, package-manifest result, source-lock result, validation result, and disposition.
2. Reconcile Builder long-form status, terminal summary, model-neutral handoff, full-evidence closure, Program Control status, VAE status, Delegation status, and XRI status without rewriting history.
3. Preserve separate states for implementation coverage, Story completion, evidence closure, external proof, production authority, and certification.
4. Register candidate authorities for AIR Constitution V2.1, AIR PRD V2.1, AHP PRD V1.3 (V1.2 plus AIR integration amendment), cross-product ownership ledger, and Format 02 deferral.
5. Register intended product roots for AIR, AHP, Interview Expression, and Studio without creating source trees.
6. Publish one semantic-object ownership matrix. AIR owns semantic lifecycle and production-program meaning; Interview Expression owns live source/reaction evidence; Builder declares dependencies; Pipeline executes; VAE realizes visual demands; Studio projects/corrects; Delegation transports.
7. Explicitly preserve `Activative Contract Compiler != Activative Intelligence Runtime`.
8. Do not add generic creative-safety/content-rights approval authority. Preserve operator-supplied source authority, provenance, lineage, approvals, and product sovereignty. Technical security remains operational.
9. Create a human ratification decision template and review packet. Do not mark candidate authority current without attributable ratification.
10. Run a fresh read-only convergence audit.

## Required outputs

- source-input lock and verification report;
- status-truth reconciliation YAML/MD;
- status conflict ledger;
- product-authority candidate registry;
- cross-product authority matrix;
- semantic-object ownership matrix;
- product-root registry;
- human ratification template and review packet;
- fresh read-only convergence audit;
- governed completion receipt and file manifest.

## Pass conditions

- all inputs verified;
- status conflicts explicit;
- no historical evidence modified;
- candidate authorities and roots registered;
- unique semantic ownership;
- Format 02 deferred;
- VAE Stage 5 unauthorized;
- no implementation/production claim implied;
- ratification state truthful.

Stop after convergence. The next step is Prompt 02.


---

# PROMPT 02 — Canonical Requirement and Specification Reconciliation

**Model:** GPT-5.6 Sol Extra High  
**Parallel:** No. It freezes the canonical writing queue.

Prepare the Canonical V2.1 Requirement, Story, Specification, Ownership, and Workflow Reconciliation Package.

Do not implement source code. Do not write the complete missing Tech Specs. Do not create contract release bytes. Do not begin Stage 5.

## Required gate

Prompt 01 must pass; required authority must be ratified or explicitly authorized for specification work; source lock and ownership/product-root registries must exist.

## Mandatory method

Load and apply the updated CA Skills:

- lifecycle controller;
- Tech Spec writer;
- independent auditor;
- reviser;
- independent re-audit/acceptance;
- one-spec builder.

## Work

1. Load and hash the AIR Constitution/PRD/Epics/Stories/20 full specs, AHP PRD/Epics/Stories/45 candidate assignments, AHP integration amendment, ownership ledger, CMF migration audit, exact source-reuse crosswalk, and current repo status.
2. Reconcile ownership conflicts. AIR owns Primitive/archetype/brand/Voice DNA/Visual DNA/Final Script/role-tension/Matrix/transfer semantic compilation. Pipeline may consume, validate, retrieve, execute, evaluate, and invalidate, but not rebuild meaning.
3. Assign exactly one disposition to every candidate specification: `ACCEPT_AS_CANONICAL`, `AMEND_TO_CURRENT_AUTHORITY`, `MERGE_INTO_CANONICAL_SPEC`, `SPLIT_FOR_IMPLEMENTATION`, `SUPERSEDE_WITH_CURRENT_SPEC`, `DEFER`, or `ARCHIVE_AS_HISTORICAL`.
4. Create canonical FR, Story, owner, and spec traceability ledgers. Every active FR has one primary owner, one primary Story, one canonical or queued full spec, one authority source, one gate, and one claim ceiling.
5. Identify every missing implementation-grade spec. Do not count assignment briefs as full specs.
6. Freeze the imported-interview reference slice from source package through semantic programs, Format 07 short, SuperVisual, animation scene package, evaluation, operator correction, HumanResolution, and replay.
7. Freeze disjoint writer lanes A–H and one-spec child-agent execution packets.
8. Initialize the Spec Quality Status Registry and Build Ledger templates from the updated Skills.
9. Create `SOURCE_DISPOSITION_LEDGER.yaml`. Only `REQUIRED_AUTHORITY`, `REQUIRED_CURRENT_IMPLEMENTATION`, and `REQUIRED_UNIQUE_EVIDENCE` sources may block when unavailable. Optional/reference-only sources create `SOURCE_GAP_NOTICE.yaml`, cannot be cited, and do not block unrelated specs. Explicitly classify missing `SRC-EXT-001` as `DEFERRED_REFERENCE` unless exact bytes are supplied and hash-locked.
10. Validate full coverage, unique ownership, no circular authority, no active Format 02, no Stage 5 authorization, and no unresolved required source path.

## Required outputs

- canonical spec ledger;
- spec disposition report;
- spec dependency DAG;
- canonical FR ledger;
- FR-to-owner/Story/spec traceability;
- full Tech Spec writing queue;
- reference-slice contract;
- writer lane manifests and path ownership;
- initialized quality registry and Build Ledger;
- validation report and completion receipt.

Stop after freezing the writing queue. The next step is Prompt 03.


---

# PROMPT 03 — Full Canonical Tech Spec Writing Factory

**Controller:** GPT-5.6 Sol Extra High  
**Child writers:** GPT-5.6 Sol High  
**Parallel:** Yes, after the controller freezes disjoint one-spec packets.

Execute the Full Canonical V2.1 Tech Spec Writing Factory.

Do not implement code. Do not create shared contract release bytes. Do not rewrite accepted AIR specs merely for uniformity. Do not let writers audit their own output.

## Required gate

Prompt 02 passes; writing queue, lane manifests, ownership, and output paths are frozen.

## Controller work

1. Validate every queued spec packet.
2. Freeze authority/source hashes and exact output path.
3. Assign one Tech Spec per child execution.
4. Enforce disjoint paths and one integration owner.
5. Provide each writer the updated `CA_TECH_SPEC_WRITE_SKILL.md` and lifecycle-controller rules.
6. Reject out-of-scope changes or missing pre-work evidence.

## Parallel lanes

- A: shared authority/contracts
- B: AIR lifecycle/integration amendments
- C: AHP kernel/retrieval/persistence
- D: Interview Expression/source packages
- E: video/static/animation runtimes
- F: Studio/revision/HumanResolution
- G: evaluation/repair/retrieval/Programmed Models
- H: VAE/Delegation/workers/operations/release

Each child writes exactly one spec and finishes with `WRITTEN_PENDING_AUDIT` plus writing/files-read/traceability receipts.

## Integration

After writers return:

- verify required ten-section structure;
- validate source references and exact target paths;
- validate FR/Story coverage;
- verify no writer issued acceptance or a capsule;
- resolve only mechanical cross-references, not architecture;
- create full spec index, traceability, writer matrix, and factory completion receipt.

Stop when every queued active spec is `WRITTEN_PENDING_AUDIT`. The next step is Prompt 04.


---

# PROMPT 04 — Independent Per-Spec Audit Factory

**Controller/arbitrator:** GPT-5.6 Sol Extra High  
**Auditors:** GPT-5.6 Sol High  
**Parallel:** Yes. Every spec can be audited concurrently by an independent agent on disjoint audit paths.

Execute the Independent V2.1 Tech Spec Audit Factory.

Do not revise specs. Do not implement code. Do not issue Development Capsules. A writer cannot audit its own spec.

## Required gate

Prompt 03 passes; each target has writing and files-read receipts and state `WRITTEN_PENDING_AUDIT`.

## Per-spec audit

Each child agent:

- audits exactly one spec;
- uses `CA_TECH_SPEC_AUDIT_SKILL.md`;
- reads controlling authority/FRs/Stories/upstream/downstream/current and predecessor source;
- runs all six audit lenses and drift blacklist;
- changes no spec bytes;
- emits audit YAML/MD/files-read receipt;
- returns `ACCEPTED_FOR_BUILD_CANDIDATE`, `REVISION_REQUIRED`, `ARCHITECT_DECISION_REQUIRED`, or `AUDIT_BLOCKED`.

## Controller work

- ensure auditor independence;
- aggregate findings;
- identify cross-spec conflicts;
- assign architecture decisions to Extra High arbitration without changing specs;
- update quality registry;
- create audit status matrix, findings registry, architecture-decision queue, and completion receipt.

Stop after audit publication. Specs requiring changes proceed to Prompt 05. Candidate passes still require Prompt 06.


---

# PROMPT 05 — Per-Spec Revision Factory

**Controller/arbitrator:** GPT-5.6 Sol Extra High  
**Revisers:** GPT-5.6 Sol High  
**Parallel:** Yes for independent specs. Shared-schema or ownership conflicts wait for one Extra High decision.

Execute the V2.1 Tech Spec Revision Factory.

Do not audit. Do not implement code. Do not issue Development Capsules.

## Required gate

Prompt 04 passes; each target has an immutable audit report and state `REVISION_REQUIRED`, or a resolved architecture decision.

## Per-spec revision

Each child:

- revises exactly one spec;
- uses `CA_TECH_SPEC_REVISE_SKILL.md`;
- reads audit findings and accepted decisions;
- fixes only findings and necessary consistency consequences;
- preserves unrelated scope;
- does not decide unresolved architecture;
- produces revised spec, context confirmation, plan, finding-resolution matrix, and revision receipt;
- ends `REVISED_PENDING_REAUDIT` or `REVISION_BLOCKED`.

## Controller work

- resolve cross-product/shared-schema decisions first;
- ensure non-overlapping file ownership;
- verify every finding has a disposition;
- update quality registry and architecture-decision ledger;
- create revision status matrix and completion receipt.

Stop after revisions. Every revised spec returns to Prompt 06.


---

# PROMPT 06 — Independent Re-Audit and Accepted-Spec Factory

**Integrator:** GPT-5.6 Sol Extra High  
**Re-auditors:** GPT-5.6 Sol High  
**Parallel:** Yes per spec. Final cross-spec convergence and hash locking are sequential under one Extra High integrator.

Execute the V2.1 Tech Spec Re-Audit and Acceptance Factory.

Do not implement code. Do not let original writers or revisers perform controlling re-audits.

## Required gate

Prompt 05 passes; revised specs have revision receipts/finding matrices. Candidate-pass specs from Prompt 04 also enter this stage.

## Per-spec work

Each re-auditor:

- processes exactly one spec;
- uses `CA_TECH_SPEC_REAUDIT_ACCEPT_SKILL.md`;
- audits the complete current spec, not only revisions;
- verifies all original findings and detects new defects;
- returns `ACCEPTED_FOR_BUILD`, `REVISION_REQUIRED`, `ARCHITECT_DECISION_REQUIRED`, or `REAUDIT_BLOCKED`;
- for accepted specs emits re-audit report and acceptance receipt.

## Integrator work

1. Run a cross-spec reference-slice trace from imported interview to replay.
2. Verify shared objects have one producer/owner and compatible consumers.
3. Reject acceptance where implementation would require invention.
4. Hash-lock accepted specs.
5. Publish accepted spec registry/hash lock, convergence report, status matrix, and completion receipt.

Any failed spec loops back to Prompt 05. Stop when the active implementation set is accepted and hash-locked.


---

# PROMPT 07 — Development Capsule and Build-Readiness Factory

**Model:** GPT-5.6 Sol Extra High  
**Parallel:** Capsule drafting may run in parallel per accepted spec on disjoint paths; final dependency/path authorization is sequential.

Create one Development Capsule per accepted implementation responsibility.

Do not implement code. Do not combine unrelated specs. Do not create a capsule for a spec without an accepted hash lock.

## Required gate

Prompt 06 passes; accepted registry/hash lock exists.

## Per-spec capsule

Each capsule contains exactly one target Tech Spec and:

- accepted spec ID/path/SHA-256;
- controlling FRs and Story;
- acceptance receipt and audit lineage;
- exact authority sources;
- exact allowed file scope and forbidden paths;
- upstream dependency receipt;
- implementation scope and non-goals;
- acceptance criteria;
- test plan;
- observability plan;
- failure/rollback plan;
- completion receipt template;
- claim ceiling.

## Controller work

- create build dependency DAG;
- create parallel path-ownership registry;
- ensure shared contracts build before consumers;
- initialize/update Build Ledger;
- assign `READY_TO_BUILD` only where accepted specs, upstream prerequisites, capsule validation, and file ownership all pass;
- publish capsule index, build wave plan, readiness report, and completion receipt.

Stop before implementation. Prompt 08 builds shared contracts first.


---

# PROMPT 08 — Shared Contract Build Wave

**Controller/integrator:** GPT-5.6 Sol Extra High  
**Per-spec builders:** GPT-5.6 Sol High  
**Parallel:** Limited. Shared canonical schemas and compatibility policy are serialized; language bindings, fixtures, and isolated consumer tests may run in parallel after schema bytes freeze.

Build the accepted shared-contract specifications using `CA_TECH_SPEC_BUILD_SKILL.md`.

Do not implement AIR/AHP application services. Do not render media. Do not begin VAE Stage 5. Do not call/train models.

## Required gate

Prompt 07 passes; each target spec is accepted/hash-locked and has a valid capsule.

## Build order

1. canonical object identities and field authority;
2. semantic schemas;
3. execution schemas;
4. compatibility/versioning/migrations;
5. canonical serialization/hash helpers;
6. generated Python/TypeScript declarations;
7. positive/negative fixtures;
8. product consumer-conformance fixtures;
9. release manifest/receipt/clean-room validation.

Each child builds one accepted spec only. Spec defects cause `BUILD_AMBIGUITY` and return to Prompt 04/05/06.

Publish a local, non-production, uncertified shared release. Stop after clean-room and consumer-conformance pass.


---

# PROMPT 09 — AIR and AHP Dependency-Controlled Build Waves

**Controller:** GPT-5.6 Sol Extra High  
**Per-spec builders:** GPT-5.6 Sol High  
**Parallel:** Yes. AIR and AHP waves run concurrently after shared contracts freeze, with disjoint product paths. Inside each product, only dependency-ready non-overlapping specs run together.

Execute accepted AIR Gate A and AHP Gate A specs using one child agent per spec and `CA_TECH_SPEC_BUILD_SKILL.md`.

## AIR wave

Build dependency-ready specs for:

- activation domains and epistemic states;
- identity/context/resonance/Matrix references;
- Primitive registry/eligibility/binding/coalitions;
- role/tension and archetype evidence/coalitions;
- Brand Context, Voice DNA, Visual DNA, RSCS/CBAR/CCV/SDA/SFL records;
- planned/observed/transfer/Final Script validation;
- HumanResolution and failure attribution;
- local persistence and receipts.

No external models or live-interview claims.

## AHP wave

Build dependency-ready specs for:

- Harness intake and semantic dependencies;
- binding manifest;
- Workflow Node kernel and scheduler;
- durable events/checkpoints/replay/cancellation;
- source-backed batch graph and local invalidation;
- authority-first exact/lexical/graph retrieval;
- JIT Capsules;
- execution fingerprints and development sandbox declarations;
- explicitly synthetic adapters.

Pipeline may not compile AIR meaning.

## Controller integration

Verify one-spec Build Receipts, file manifests, tests, path ownership, dependency updates, and product status. AIR and AHP maximum claims remain deterministic development foundations, not production.

Stop when Gate A accepted builds and integration tests pass.


---

# PROMPT 10 — Imported Interview and Semantic Production Vertical

**Controller/integrator:** GPT-5.6 Sol Extra High  
**Child builders:** GPT-5.6 Sol High  
**Parallel:** Bounded. Media/transcript/shot-map implementation and operator-review UI may run in parallel after source schema/IDs freeze. Semantic finalization waits for approved Expression Moment.

Build accepted specs for one imported talking-head interview through an operator-approved semantic production package.

Do not conduct a live interview. Do not render final derivatives. Do not activate Format 02. Do not let models auto-approve meaning.

## Flow

1. media hash/probe and source record;
2. transcript ingestion, word/speaker alignment, packed phrases, silence/audio events;
3. shot boundaries/transitions/keyframes;
4. tag epistemic states and imported admission mode;
5. Expression Moment candidates and operator approval;
6. Canonical Interview Source Package and Observed Activative Intelligence Pack;
7. diverse activation hypothesis portfolio and operator selection;
8. Matrix of Edging resolution;
9. exact Primitive candidate retrieval, bindings, coalition, signature, Edge Product, misuse risks, receipt;
10. explicit psychological role inside a tension;
11. archetype candidates/coalition using current authority;
12. Brand Context, Guest Voice DNA, Visual DNA, RSCS/CBAR/CCV/SDA/SFL;
13. source-lineaged Final Script and operator approval;
14. Activation Transfer Contract;
15. semantic programs for Format 07 short, SuperVisual, and reusable non-Format-02 animation scene package;
16. immutable AHP handoff proving Pipeline consumption without semantic mutation.

Each implementation child builds one accepted spec only. Operator decisions are actual gates, not model substitutions.

Stop before rendering with maximum claim `REFERENCE_SEMANTIC_PRODUCTION_PACKAGE_OPERATOR_APPROVED`.


---

# PROMPT 11 — Rendered Reference Batch, Studio Correction, and HumanResolution Proof

**Controller/integrator:** GPT-5.6 Sol Extra High  
**Per-spec builders:** GPT-5.6 Sol High  
**Parallel:** Runtime-specific builds (FFmpeg, static Skia, bounded Remotion/HyperFrames, Studio read models) may run concurrently only after accepted shared inputs and non-overlapping paths freeze. Final batch, operator correction, selective rerun, and replay are sequential.

Execute the accepted build specs that prove one source-first reference batch.

Do not build the complete platform. Do not begin VAE Stage 5. Do not use GNM. Do not activate Format 02. Do not fine-tune models.

## Required outcome

- canonical VideoEditProgram adopted from predecessor edit/timeline contracts;
- word-boundary A-roll EDL with original talking-head footage/voice as base;
- FFmpeg source-led short with captions and one bounded evidence/motion slot;
- one real SuperVisual using Composition IR/PRETEXT/geometry/Skia;
- persisted reusable animation scene package, rendering only required scenes;
- rendered technical and semantic evaluation including Activation Transfer Fidelity, Primitive coalition expression, role/tension, Negative Space, and Edge Integrity;
- bounded local repair;
- Studio projection of source, semantic programs, campaign graph, video, SuperVisual, animation package, receipts, and timeline;
- one exact operator revision compiled into ChangeRequestProgram;
- descendant-only selective rerun and re-evaluation;
- immutable HumanResolutionEpisode with before/after/actions/scope/attribution/evidence;
- retrieval indexing without live weight/Skill/doctrine mutation;
- audit/replay export and checkpoint replay.

Every child build targets one accepted spec. Final integration is accepted only when real files and hashes exist and synthetic evidence is explicitly separated.

Maximum claim: `SOURCE_FIRST_REFERENCE_SLICE_ACCEPTED_DEVELOPMENT_EVIDENCE`.

Stop after acceptance or an exact typed blocker. Do not begin the next expansion wave.
