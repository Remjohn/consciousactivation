# Audience Context Program Instructions

1. Verify workspace isolation and active tenant session.
2. Initialize Audience Profile (`initialize_audience`) under COMMANDER supervision.
3. Hunt for audience tensions and acute resistances (`hunt_tensions`) in the HUNTER lane.
4. Map and verify protected Cognitive Islands (`map_cognitive_islands`) in the ANALYST lane with source evidence hashes.
5. Project mutable current-state coordinates and viewer state sequence (`project_current_state`) in the COMPOSER lane.
6. Await human Operator Gate authorization (`approve_audience_context`) before activating audience context.
