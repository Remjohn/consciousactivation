# Builder Start Here

This is the single active Atomic Harness Builder repository in the Conscious
Activations workspace.

Before changing files:

1. read `AGENTS.md`;
2. read `docs/product-authority/CURRENT_AUTHORITY.md`;
3. read `CURRENT_PROJECT_STATUS.md` and `PROGRAM_STATUS_EXPORT.yaml`;
4. identify the governing confirmed Story or supplemental amendment;
5. validate its dependency receipts, blocker cut, Development Capsule, manifest,
   implementation authorization, and exact file allowlist; and
6. stop when any authority, integrity, dependency, blocker, or scope gate fails.

Constitutional Alignment Batch A is completed historical work under
`docs/constitutional-alignment/`; it is no longer the active repository mission.
Preserve its receipt and limitations without using its old write restriction to deny
later governed Story receipts.

Current state: 27 of 69 confirmed Stories have full StoryCompletionReceipts. The active
`OD-AM-001` campaign may implement only its exact 22 offline development targets and
issue separate ImplementationCompletionReceipts. BD-007 and all external evidence,
production, certification, provider, cloud, VAE, Delegation, and real-human-reaction
gates remain open at their original scopes.

The program-level source of truth remains in `../CMF_PROGRAM_CONTROL/`. Do not modify
Program Control, Visual Asset Editor, or Delegation Protocol authority from this
repository.
