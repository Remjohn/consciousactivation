# Test Plan

Test separate answer and decision identity, human authority, exact option/path/lineage,
atomic amendment and memory commit, cascade state, provisional distinction, resume,
idempotency, replayed-approval rejection, contradiction, stale/invalidated input,
reopening, descendant invalidation, outbox retry, deterministic fresh-context bytes,
zero partial state and exact architecture boundaries. Run Story tests twice, affected
architecture/correction suites, full repository regression and Python compilation.
