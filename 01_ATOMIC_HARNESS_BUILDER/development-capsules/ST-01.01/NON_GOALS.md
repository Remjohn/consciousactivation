# Non-Goals

ST-01.01 does not:

- implement ST-01.02 or any later Story;
- ingest, normalize, lock, index, retain, redact, withdraw or evaluate evidence;
- collect Human Reaction material or store Identity DNA amendment proposals;
- implement conversational `public_comment`, `reply_dm`, `reelcast_expression` or `interview_expression` execution;
- author or approve the Interview Expression or ReelCast final product PRD;
- implement visual syntax, temporal syntax, grammar induction, atomicity or Genesis;
- create Harness IR, Workflow IR, product graphs, capability registries, skills, JIT capsules or target packages;
- implement a workflow scheduler, Temporal adapter, worker, retry system, incident process or agent runtime;
- implement a database, migration, PostgreSQL adapter, S3 adapter, production CAS or deployment;
- implement API, CLI, Control Tower or UI behavior;
- implement VAE behavior, asset realization, wrong-reading-lock enforcement, or any external editor runtime;
- implement Delegation behavior, shared schemas, source-kind logic, handoff execution, or a local Delegation schema fork;
- import or execute Delegation `1.1.0-rc.4`;
- benchmark, certify or claim production readiness for Format 02;
- inherit Format 02 status into any conversational profile;
- resolve HD-006, HD-007, BD-004, BD-007, BD-008, BD-010, BD-014 or any XRI;
- change the confirmed 410 obligations, 12 Epics or 69 Stories;
- change any PRD, Constitution, registry, contract, schema, technical specification, ADR or planning baseline; or
- authorize itself, Release 1, the full product, VAE, Delegation or any production deployment.

