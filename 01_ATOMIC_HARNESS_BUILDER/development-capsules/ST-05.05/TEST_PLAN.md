# Test Plan

Test full transition order, exact hash pins, no silent upgrades, missing pins, authorization, disposal, invalidation, historical reproduction, duplicate idempotency, conflicting payloads, replay-safe state, injected atomic failure, typed observations, deterministic receipts, predecessor regression, full regression twice, Python compile, receipt/manifest verification, and absolute-path/secret scans.
