# Observability Plan

Every application command produces a success or typed rejection observation with story, operation, command, actor, artifact identity, outcome, and failure context. Lifecycle receipts bind exact capsule and package hashes without semantic payload or secrets.
