# ST-01.04 Implementation Authorization

Verdict: `PASS` under `AUTHORIZE FULL ATOMIC HARNESS BUILDER IMPLEMENTATION CAMPAIGN`.

The category-neutral outcome is independently implementable after the validated
`ST-01.03` receipt. Its blocker cut is empty: `BD-004` is not activated without a
real profile corpus, and `HD-006` is not activated without Human Reaction material.

Implementation is limited to the exact deny-by-default allowlist. It may evaluate
the local synthetic Saturation Contract, attach an immutable typed outcome, and
provide deterministic replay, invalidation, observations and rollback. It may not
advance atomicity or Genesis, authorize readiness, implement external behavior, or
make production/certification claims.
