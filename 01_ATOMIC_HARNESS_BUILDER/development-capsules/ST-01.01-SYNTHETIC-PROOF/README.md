# ST-01.01-SYNTHETIC-PROOF Development Capsule

This capsule authorizes no implementation by itself. It bounds a separately receipted, additive extension of completed Story `ST-01.01` so the governed run lifecycle can admit one repository-owned, category-neutral synthetic Builder proof profile.

The branch is synthetic, non-production, non-certified, and limited to Builder Core validation. It creates no sixth category, no additional compilation target, and no primary obligation owner. The original `ST-01.01` implementation and PASS completion receipt remain authoritative and immutable.

Implementation may begin only after the human states exactly:

`AUTHORIZE BUILDER ST-01.01-SYNTHETIC-PROOF BOUNDED IMPLEMENTATION`

The authorized branch may add a synthetic proof profile adapter and the tests needed to preserve the existing fail-closed lifecycle behavior. It must not begin `ST-01.02`, compile an Atomic Harness, implement external product behavior, or claim certification.

The immutable capsule inputs and their hashes are recorded in `CAPSULE_MANIFEST.json`. The authorization gate is recorded in `IMPLEMENTATION_AUTHORIZATION.yaml` and still requires the phrase above.
