# Implementation Scope

Add a pure domain compiler for immutable category-local profile records and an
application command boundary with deterministic receipts, authority checks,
idempotency, invalidation-safe identity, and atomic failure behavior.

The output must include the seven edited-video profile IDs under
`short_form_edited_video`, `format02_minimal_coach_theatre` under
`2d_character_animation`, and the four governed conversational structural profiles.
Format 02 must declare every FR-144 registry requirement. Empty-profile categories
remain explicit. No profile runtime, syntax parsing, real state, corpus, provider,
benchmark, or certification work is included.

