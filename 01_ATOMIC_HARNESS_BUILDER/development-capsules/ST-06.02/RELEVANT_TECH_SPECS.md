# Relevant Technical Specifications

- TS-00 constitutional precedence remains mandatory through the current authority chain.
- TS-11 owns `FormatProfile`, category/profile compilation, deterministic identity,
  category isolation, structural conformance, and external-runtime prohibitions.
- TS-15 governs Format 02 as a reference profile whose current strongest state is
  `contract_compatible`, not benchmarked or certified.

Implementation owner: `category_and_target_compiler_owner`. Component boundary:
Builder-owned structural profile registry and command service only.

