# Relevant Accepted ADRs

- `ADR-003`: atomic command, event, receipt, projection and outbox persistence.
- `ADR-005`: code computes eligibility, agents may propose, humans own final decisions.
- `ADR-012`: deny-by-default, read-only evidence and least privilege.

The Story does not revise an ADR or activate production persistence, external products,
protected labels, certification or a shared schema.
