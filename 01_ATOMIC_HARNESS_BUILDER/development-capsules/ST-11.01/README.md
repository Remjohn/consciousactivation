# ST-11.01 Development Capsule

This capsule authorizes only `ST-11.01 / SYNTHETIC_BUILDER_PROOF`: generate one immutable, versioned and traceable Development Capsule for the validated `synthetic_text_normalization_v1` Atomic Harness Definition.

The generated capsule is a Builder proof artifact. It gives a downstream implementation agent complete governed authority for this synthetic Harness without inventing product decisions, but it does not implement that Harness or authorize production use.

Read and validate every immutable input, `CAPSULE_MANIFEST.json`, and `IMPLEMENTATION_AUTHORIZATION.yaml` before editing. Work only within `ALLOWED_FILE_SCOPE.yaml`.

Human authority is the standing campaign phrase `AUTHORIZE BUILDER SYNTHETIC DEMONSTRATION COMPLETION CAMPAIGN`. It applies because ST-07.04 has a PASS receipt, this capsule uses `SYNTHETIC_BUILDER_PROOF`, the blocker cut is empty, and the scope is limited to generating and validating the synthetic proof capsule.
