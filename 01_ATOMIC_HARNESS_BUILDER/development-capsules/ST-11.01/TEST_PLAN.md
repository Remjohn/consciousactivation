# Test Plan

Run with repository `PYTHONPATH` and no mandatory skips.

- Acceptance: successful capsule generation, all 15 required sections, six owned obligations, typed references, justified scaffold, examples, fixtures, tests, completion-receipt contract, and bounded classification.
- Traceability: exact definition/validation/authority/dependency lineage, complete hashes, section/reference ordering, portability, deterministic fresh-context byte equality, and changed-input identity.
- Failures: missing/unhashed/mutable reference, incomplete obligation trace, unresolved contract, unjustified scaffold, missing fixture/test, external behavior, production/certification claim, altered or invalidated lineage, unauthorized actor, conflicting command, and injected atomic failure.
- Lifecycle: duplicate-command idempotency, replay/resume, upstream invalidation, historical reproduction, clean retry, and non-destructive rollback.
- Architecture: no external dependency, network, database, schema fork, external runtime, product implementation, UI, API, Control Tower, Format 02, VAE or Delegation behavior.
- Regression: all predecessor tests and the complete repository suite twice at campaign completion.

Success requires zero failures and zero mandatory skips.
