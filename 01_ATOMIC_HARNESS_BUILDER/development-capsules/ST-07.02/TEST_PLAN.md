# Test Plan

Require the then-current governed complete repository regression, initially `404/404 PASS` with no mandatory skips.

Story tests must verify:

- successful compilation of the exact synthetic `AtomicHarnessDefinition`;
- all capsule-required sections and exact four-obligation coverage;
- exact source profile, Source Lock, Harness IR, ratification, precedence, artifact-set, module, phase, handoff, context, registry, and necessity lineage;
- explicit target/output/acceptance-test contracts;
- category adapter `NOT_APPLICABLE_SYNTHETIC_PROOF` and `synthetic_not_certifiable` enforcement;
- absence of Format 02 and external-product/runtime dependencies;
- altered, missing, stale, superseded, invalidated, wrong-version, foreign-target, category-bound, incomplete, or unauthorized input rejection;
- deterministic canonical ordering, fresh-context byte equality, definition/hash/receipt reproduction, and changed-input identity;
- payload-safe idempotency and conflicting-command rejection;
- atomic failure with zero partial state and clean retry;
- replay, checkpoint resume, upstream invalidation, active-state rejection, and historical reproduction;
- deterministic observations and architecture boundaries;
- every predecessor regression.

No Harness execution, workflow runtime, Development Capsule generation, network, provider, database, GPU, evaluator, VAE, Delegation runtime, Format 02, conversational, or production fixture is permitted.

