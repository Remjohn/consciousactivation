schema_version: cmf-builder-vae-validation-request-package/v1
status: PREPARED_NOT_EXECUTED
request_scope:
  builder_owned: [port_contract, request_mapping, result_mapping, failure_containment, receipt_validation]
  external_owned: [visual_asset_editor_runtime_behavior, image_generation, production_rendering]
required_return:
  - VAE version identity
  - accepted request receipt
  - result or rejection receipt
  - compatibility report
  - drift and invalidation report
prohibited:
  - executing VAE from EC-AM-001
  - claiming VAE production validation

