# BD-007 later execution plan

The later campaign should expose one governed command that performs, in order:

1. validate operator cloud authority;
2. validate mixed-governed-source case-binding amendment;
3. resolve and hash the immutable model revision;
4. verify model bytes and container digest;
5. deploy one endpoint;
6. run 24 semantic calls;
7. score all 17 dimensions and nine non-compensable failures;
8. tear down the endpoint;
9. emit BD-007 execution, scorecard, limitation and disposition receipts.

The command must stop before inference when any case evidence, model, runtime, budget, security or retention gate fails.

