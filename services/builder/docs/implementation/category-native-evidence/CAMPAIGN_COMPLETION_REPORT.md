# BD-004 Corpus Admission and BD-007 Governed Provider Evidence Campaign

Verdict: **BLOCKED_EVIDENCE — valid authority stop**

The campaign completed every authorized preparatory and infrastructure outcome but did
not close either blocker and therefore did not start ST-06.03 or ST-06.04.

## BD-004

The exact archive remains hash-valid. All 1,434 members are partitioned exactly once:
48 canonical textual/structured development-evidence members are proposed for
admission and 1,386 are excluded. The admitted proposal hash is
`4321a232dee1dc87ac5a97d2b8e9ba115dec52558b7d5abc13cee1f851bfb70a`;
the exclusion hash is
`3947182c54757842bd9be933f50b84ed5454184686cbc711141f5eff6d337141`.
All 48 proposed member byte hashes reproduce from the immutable archive.

No operator identity, authority basis, effective date, or signature/approval receipt
was supplied. The proposal is not an admitted corpus and none of its members may be
used in an empirical provider trial. BD-004 remains `BLOCKED_OPERATOR_AUTHORITY`.

## BD-007

The new provider-neutral evidence runner validates immutable configuration, exact
executable/package/runtime/model identities, deterministic controls, tool/network
policy, credential-name-only handling, bounded calls/retries, timeout/cancellation,
output capture, and drift rejection. The local fixture proves runner mechanics only;
it is not a semantic provider.

The paired experiment system freezes four case families, 17 evaluation dimensions,
nine non-compensable failures, minimum three repeats per arm, hash-bound requests and
scorecards, and a non-Activative control branch. It has produced no empirical result.

There are zero eligible semantic-provider configurations, authorized calls, provider
outputs, or scorecards. The exact model/configuration, credential/account authority,
USD cap, data transmission, network allowlist, output limit, and retention policy are
still missing. BD-007 remains `BLOCKED_EVIDENCE`.

## Story effect

- ST-06.03: `BLOCKED_EVIDENCE` by `[BD-004, BD-007]`; no implementation or receipt.
- ST-06.04: `BLOCKED_PRIOR_STORY`; no capsule or implementation activated.
- ST-06.05: excluded from this campaign and not implemented.
- Confirmed Story inventory: `27/69` complete, `42` remaining.
- Production readiness: false.
- Certification: false.

## Validation

- baseline: `909/909 PASS`;
- evidence infrastructure and focused architecture: `61/61 PASS`;
- final regression: `959/959 PASS` twice from fresh processes;
- Python source and test compilation: PASS;
- 25 YAML and two JSON evidence documents: PASS;
- embedded hashes, portable-source path scan, and secret-value scan: PASS.

The next executable action is human evidence, not Story implementation: truthfully
execute the exact BD-004 declaration, then supply an exact provider configuration and
bounded credential/spend/transmission/retention authority. Empirical trials may run
only after both gates pass.
