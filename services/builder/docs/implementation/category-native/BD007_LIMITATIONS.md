# BD-007 ST-06.03 Empirical Baseline Limitations

Date: 2026-07-16  
Scope: `ST-06.03 / Compile Category-Native Syntax and Activative Sequencing`

## Result

No empirical baseline was executed. The exact result classifications are:

- archive/case admission: `BLOCKED_OPERATOR_AUTHORITY`;
- eligible provider/configuration: `BLOCKED_EXTERNAL_PROVIDER`;
- empirical category-native versus flattened-generic comparison: `BLOCKED_EXTERNAL_PROVIDER`;
- production and certification branches: `NOT_APPLICABLE_TO_THIS_BRANCH`.

This is not a failed model comparison. It is a governed refusal to produce a model
comparison without admissible inputs and an eligible execution surface.

## Archive limitation

The recovered archive has the expected immutable SHA-256
`403bd07f8ca3feae94a991b16a08270fb799ffea87a05ab108163b4e1dee37b0`, but no archive
member was admitted into this lane. The BD-004 qualification lane found that operator
usage/adoption authority was not already governed. Consequently:

- zero archive members were materialized as cases;
- zero archive bytes were sent to a provider;
- no internal archive statement was treated as operator legal authority;
- no archive example was relabelled as a protected, benchmark, production, or
  certification case.

## Provider limitation

The local process could see an OpenAI Python CLI installation at version `1.59.7`,
LiteLLM `1.58.0`, and the Codex desktop command surface. None is an eligible Builder
provider configuration:

- the inspected environment exposes no supported provider credential variable;
- the repository contains no governed visual-inference provider adapter or comparison
  runner;
- LiteLLM has no configured, immutable backend and is only a routing client;
- no immutable model/configuration policy or authorized remote-data policy is bound;
- Codex desktop is an application-managed development surface, not a reproducible
  Builder benchmark adapter;
- no Ollama or LM Studio model runtime/model inventory was available.

No credential value was read or recorded and no network/provider request was made.

## Empirical and threshold limitation

There are no outputs, scores, repeats, outliers, confidence measures, or provider
failures to evaluate. Therefore this lane establishes no numeric development threshold
and no provider ranking. It cannot demonstrate that category-native compilation
preserves Activative meaning better than a flattened generic projection.

The protocol records a development-only, non-compensable decision rule. It may be
used only after admitted cases produce complete paired evidence. Production thresholds,
provider selection, benchmark eligibility, and certification remain outside this
disposition.

## Exact later execution package

The following must exist before the comparison can run:

1. A truthful operator authority declaration bound to the exact archive hash, purpose,
   development-only use, provenance limitations, adoption date, authority identity,
   and non-certification status.
2. A BD-004 admission receipt listing every permitted member and immutable member hash.
3. A materialized case manifest generated only from those admitted members, with
   category/profile identity, provenance, usage authority, rich Activative lineage,
   wrong-reading locks, and isolated expected evidence.
4. At least one eligible, immutable provider configuration for an internal baseline;
   two materially distinct eligible configurations are required to close the broader
   provider-comparison branch.
5. For each provider configuration: provider/model identity, immutable version,
   configuration hash, invocation policy, authorized credential source, data/privacy
   authorization, deterministic-control support, output schema, and typed failure
   contract.
6. A governed provider adapter and runner implementing `AdmitCorpus`,
   `MaterializeBlindedCases`, `RunPairedProjection`, `EvaluatePairedProjection`, and
   `DecideDevelopmentEvidence` exactly as specified in
   `BD007_BASELINE_PROTOCOL.yaml`.
7. An independent evaluator and label-custody policy that prevents generator access to
   protected expectations and records per-dimension evidence, contradictions, repeats,
   outliers, latency, cost, and typed failures.
8. Three fresh-process repeats per case, arm, and eligible configuration, with raw and
   normalized output hashes and complete invocation/evaluation receipts.

There is intentionally no executable shell command in this package. No governed runner
exists today, so publishing a command as executable would fabricate repository
capability. Once a runner is separately authorized and hash-pinned, its receipt must
prove that it implements the five command contracts above before it may produce BD-007
evidence.

## Claims explicitly not made

- No provider is preferred, selected, benchmarked, or certified.
- No development, production, or certification threshold passed.
- No Format 02 or conversational profile is production ready.
- No VAE, Delegation, Interview Expression, ReelCast, or external-provider behavior was
  implemented.
- Production readiness and certification remain `false`.
