# Atomic Harness Builder RC1 limitations

RC1 is not accepted for clean installation because the repository has no governed package metadata or dependency lock. The source tree is locally usable with `PYTHONPATH=src`, and the source-based operator path passes for the generic and Activative fixtures, but that is not a clean-environment release.

Open gates remain BD-007 cloud/provider empirical evidence, BD-008 protected Format 02 evidence, external VAE/Delegation validation, real human-reaction evidence where applicable, production authorization and certification evidence.

Production readiness and certification remain false.

