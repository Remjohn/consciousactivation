# Implementation Scope

Assemble an immutable capsule containing the exact evaluated package pin, minimum required phase context, authority, contracts, acceptance tests, failure/stop conditions, observability, rollback, wrong-reading locks, and semantic lineage. Reject optional, forbidden, inapplicable, unknown, stale, or unpinned context.
