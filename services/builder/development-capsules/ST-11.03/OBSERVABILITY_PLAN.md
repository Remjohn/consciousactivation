# Observability plan

Emit `implementation_feedback_ingestion_started`, one
`implementation_feedback_item_governed` event per typed item,
`implementation_feedback_proposal_committed`, replay and rejection events. Bind run,
Story, proposal identity, plan and evidence provenance, authority, outcome, command,
correlation, causation and typed failure context.

