# Observability Plan

Emit `ST-03.02:HumanDecisionRecorded`, `ST-03.02:OutcomeVerified`, typed rejection,
replay, resume and invalidation observations. Each includes run, Story, question,
answer, decision, amendment, human authority, model/graph/package lineage, outcome and
failure context. Successful intents use the atomic outbox.
