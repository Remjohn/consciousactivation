# ST-01.02 Development Capsule

Status: `VALIDATED_AWAITING_HUMAN_IMPLEMENTATION_AUTHORIZATION`

This capsule is the complete bounded implementation context for the
`SYNTHETIC_DEFINITION` mode of confirmed Story `ST-01.02`, **Lock a Safe
Target-Specific Evidence Workspace**.

The outcome is category-neutral: an authorized Builder run binds the exact
repository-owned synthetic task definition to a versioned source profile,
diagnoses it without mutation, and commits a deterministic immutable Source
Lock with actionable failure evidence.

The capsule does not authorize implementation. The only phrase that may open
this bounded implementation gate is:

`AUTHORIZE BUILDER ST-01.02 SYNTHETIC-DEFINITION BOUNDED IMPLEMENTATION`

The implementation must remain inside `ALLOWED_FILE_SCOPE.yaml`. It must not
implement Format 02, VAE, Delegation runtime, GPU/provider behavior,
conversational policies, evaluators, certification, or later Stories.

Full Release 1 and full-product readiness remain `FAIL`.
