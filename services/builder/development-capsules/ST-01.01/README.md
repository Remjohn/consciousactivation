# ST-01.01 Development Capsule

This capsule bounds implementation of the confirmed Story **Start and Resume One Target-Profiled Builder Run** to the non-conversational Format 02 reference path.

The permitted outcome is a deterministic, replay-safe run-governance slice with stable identity, explicit actor authority, exactly one selected target profile, typed lifecycle state, immutable events, and resume from the latest valid checkpoint. It may recognize the three governed compilation-target identities, but only `atomic_content_harness` with `2d_character_animation/format02_minimal_coach_theatre` is executable under this authorization package.

This capsule is valid while full Release 1 readiness and full-product readiness remain `FAIL`. Its bounded readiness does not confer implementation authority. Implementation may start only after a human supplies the exact phrase recorded in `IMPLEMENTATION_AUTHORIZATION.yaml`, while every capsule hash still matches `CAPSULE_MANIFEST.json`.

No evidence ingestion, visual understanding, Genesis, Harness IR compilation, workflow runtime, Control Tower, target-package compilation, VAE behavior, Delegation behavior, external service, production persistence, or certification is in scope.

Read in this order:

1. `STORY_CONTRACT.yaml`
2. `OWNED_REQUIREMENTS.csv`
3. `RELEVANT_TECH_SPECS.md` and `RELEVANT_ADRS.md`
4. `IMPLEMENTATION_SCOPE.md` and `ALLOWED_FILE_SCOPE.yaml`
5. acceptance, test, observability, failure, security, and non-goal plans
6. `DEPENDENCY_RECEIPT.yaml`
7. `CAPSULE_MANIFEST.json` and `CAPSULE_VALIDATION_REPORT.md`
8. `IMPLEMENTATION_AUTHORIZATION.yaml`

