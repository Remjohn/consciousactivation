# Relevant Technical Specifications

- TS-00 preserves the current constitutional and product authority chain and forbids a
  lower-authority compiler projection from weakening its source.
- TS-03 governs Visual Syntax First as development-time evidence discovery. It cannot
  become runtime semantic invention.
- TS-05, TS-06, and TS-07 require immutable upstream lineage, typed responsibility
  boundaries, deterministic derived identity, and descendant invalidation.
- TS-09 preserves exact skill/JIT evidence references without making skill execution
  part of this Story.
- TS-11 owns category syntax, sequence programs, category isolation, deterministic
  compilation, and Builder/external-product boundaries.
- TS-14 preserves Activation First as runtime law; ST-06.03 compiles declarations and
  does not execute a workflow.
- TS-15 keeps Format 02 a non-certified reference profile and prohibits certification
  inheritance.

Implementation owner: `category_and_target_compiler_owner`. Component boundary:
Builder-owned category-native syntax and sequence compilation only. Live Interview,
ReelCast, VAE, Delegation, provider, and Harness runtime behavior remain external.

