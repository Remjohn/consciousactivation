# ST-03.03 Development Capsule

This capsule governs only confirmed Story `ST-03.03`, “Maintain One Provenance-Rich Canonical Harness IR,” in the confirmed `PRE_RATIFIED_SYNTHETIC_INPUT` mode.

The bounded outcome is one immutable, versioned, category-neutral `HarnessIR` revision compiled from the exact active Source Lock, frozen atomic boundary, human ratification, and Draft Harness Model produced by the completed Builder Core path. Every material value retains authority, knowledge status, provenance, dependency impact, and explicit synthetic/non-production scope. The IR is the sole product-semantic source of truth and remains separate from Workflow IR.

This capsule does not implement human/machine artifact compilation (`ST-03.04`), constitutional cross-artifact validation (`ST-03.05`), an Atomic Harness Definition, task execution, external target behavior, or production certification.

Implementation is not authorized until the human supplies exactly:

`AUTHORIZE BUILDER ST-03.03 PRE-RATIFIED-SYNTHETIC-IR BOUNDED IMPLEMENTATION`

The phrase authorizes this Story only. Run tests with `PYTHONPATH=src`; the current verified repository baseline is `84 passed`.
