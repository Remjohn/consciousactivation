# Security and Authority Boundaries

Only Builder-owned deterministic code with an active `COMPILE_CATEGORY_PROFILES` grant
may commit the derived structural registry. Humans remain the authority over category
constitutions. Inputs are read-only hash-pinned evidence. No network, credentials,
external product behavior, real-person data, provider output, or production state is used.

