# Failure and Rollback

All validation precedes the state/command commit. Injected failure preserves prior state and records no command. Disposal and invalidation change active lifecycle state without deleting immutable capsule bytes. Rollback is non-destructive and historical reproduction remains hash-verified.
