# Implementation Scope

Implement an atomic Builder-owned lifecycle for the exact immutable capsule and package hashes. Prevent silent upgrades, unpinned use, invalidated reuse, conflicting commands, and unsafe cleanup while retaining historical bytes and deterministic receipts.
