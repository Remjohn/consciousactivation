# ST-03.01 Development Capsule

This bounded capsule implements category-neutral dependency-ready Genesis question
selection. It compiles supplied versioned decision definitions into one immutable,
evidence-backed question package; it does not answer, ratify or commit the decision.

Authority: Activative Intelligence Constitution V1.1, Builder PRD V1.2, the
confirmed inventory and Builder-first overlay, plus standing phrase
`AUTHORIZE FULL ATOMIC HARNESS BUILDER IMPLEMENTATION CAMPAIGN`.

Direct dependencies are the validated PASS receipts for `ST-01.04` and `ST-02.05`.
`BD-004` is inactive without a real corpus and `BD-008` is certification-only.
