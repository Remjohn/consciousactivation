# Execution instructions

1. Verify the exact package identity and the input contract.
2. Check immutable references, authority precedence, and source attribution.
3. Confirm that Identity DNA is reference-only and that audience claims retain their
   hypothesis status.
4. Resolve the edge from the governed Matrix of Edging and Live Premise evidence.
5. Preserve the requested format goal, roles, call constraints, desired reaction, and
   micro-commitment without adding ungrounded meaning.
6. Apply the behavioral anchors and block every declared wrong reading.
7. Preserve `APPLICABLE` and `NOT_APPLICABLE` decisions with non-empty reasons.
8. Attach exact lineage to every activation-field output.
9. Validate the output contract and emit deterministic package observations.

If a required input is missing or contradictory, stop with the typed failure instead
of guessing. The output remains a development artifact and does not authorize runtime
execution.
