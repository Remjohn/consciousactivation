# CMF Visual Asset Editor Cross-Repository Issues

Date: 2026-07-14  
Status: open through Stage 4 readiness; resolved rows are retained as audit history

No issue in this register authorizes the Visual Asset Editor to change an upstream contract, duplicate an upstream component, or silently choose shared lifecycle semantics.

| ID | Conflict or missing dependency | Repository owner | Local evidence | Impact | Required resolution | Status |
|---|---|---|---|---|---|---|
| CR-001 | The exact frozen Atomic Harness Builder PRD and executable Spec Builder V2.1 sources are recovered, but no target Atomic Harness Builder runtime checkout exists. The Spec Builder's scaffold writes specification pointers; it does not expose Harness IR runtime, Workflow Runtime, JIT execution, Control Tower, repair, event-store, or Development Capsule extension points. | Atomic Harness Builder | Exact-hash SRC-001/SRC-002; `validation/BUILDER_EVIDENCE_RECONCILIATION_2026-07-14.md`; Spec Builder test suite `28 passed` | Blocks verified runtime/Control Tower attachment, architecture-preservation proof, recovery integration, collision testing, and Development Capsule completion. | Supply a tagged/read-only target runtime checkout and identify the exact target profile, version, hashes, symbols, contracts, and extension points. | PARTIALLY MITIGATED - BLOCKING IMPLEMENTATION AUTHORIZATION |
| CR-002 | The Delegation workspace now contains a coherent unsigned local `1.0.0-rc.2` package with 26 messages, generated bindings, package validator PASS and 42 tests PASS, but it is not a published/signed production dependency and explicitly keeps Stage 5 unauthorized. | Delegation repository | Exact RC2 digests in `DELEGATION_DEPENDENCY.md`; live package validator/tests; current 26-message matrix PASS against schema and authority registry | RC2 is strong local evidence but cannot be treated as an immutable owner-ratified production pin. | Fix CR-007, publish/sign, provide immutable coordinate/digest, and execute VAE producer/consumer/adapter conformance. | MITIGATED BY COHERENT LOCAL RC2 - BLOCKING IMPLEMENTATION AUTHORIZATION |
| CR-003 | Five shared-boundary schema seeds live under the editor package even though shared Delegation schemas are externally owned. RC demand/event require adapters and the local result representation requires immutable migration; local snapshots remain prohibited as public bindings. | Delegation repository with VAE adapter ownership | VAE shared schema snapshots; RC schemas; `docs/contracts/CONTRACT_COMPATIBILITY_MATRIX.yaml`; `docs/contracts/ADAPTER_TEST_PLAN.md` | Risk of editor-owned fork, dropped identity, arbitrary public state, incorrect result authority, and incompatible amendment handling. | Keep VAE files excluded from production build inputs; use only generated published bindings after authorization; prove adapters and result migration with executable fixtures. | MITIGATED BY DESIGN - BLOCKING PUBLISHED BINDINGS AND CONFORMANCE |
| CR-004 | The Format 02 Content Harness and downstream Remotion composition consumer are absent. The editor's geometry and Asset Result obligations cannot be tested against a real consumer. | Format 02 Content Harness / composition runtime | `reference-slice/FORMAT02_MINIMAL_COACH_THEATRE/README.md`; benchmark `release_proof`; `docs/implementation/FORMAT02_RELEASE_PLAN.md` | Blocks end-to-end geometry, timing/sequence authority, composition-effectiveness, acknowledgement, usage, and accepted-result consumption proof. | Supply the pinned format profile, consumer contract, representative demand producer, composition fixture, and executable acceptance path. | OPEN - BLOCKING IMPLEMENTATION AUTHORIZATION |
| CR-005 | Nine of ten local source artifacts were found by exact SHA-256 and governed paths were rebased. `SRC-009` remains absent after scanning 2,618 text files plus the attachment and Codex artifact stores. | Source-package owners / release curator | `validation/SOURCE_PATH_REBASE_2026-07-14.md`; live package validator | Only the missing AI developer workflow transcript prevents complete local source-integrity PASS. | Restore `SRC-009` with registered SHA-256 `92811d394f18a533efda53b216b048da21efba6b297f14c8d64cbf29547dc143`; do not substitute similar text. | NARROWED TO SRC-009 - BLOCKING PACKAGE APPROVAL |
| CR-006 | The current matrix covers all 26 RC2 registry messages, exact schema hashes, and allowed verdicts; the matrix validator passes with 20 compatible, 4 adapter, 1 migration-required, and 1 incompatible path. | Delegation repository with VAE adapter ownership | `scripts/validate_contract_matrix.py`; current matrix; RC2 registry | The stale 25-message reconciliation gap is closed for the observed RC2 registry; it must be repeated for a coherent signed release. | Re-run the same mechanical reconciliation when the package is rebuilt or published. | RESOLVED FOR OBSERVED RC2 REGISTRY - RECHECK REQUIRED |
| CR-007 | RC2 correctly assigns Content Harness as `amendment-response` producer but omits VAE from consumers, despite VAE being the proposal producer and the Delegation ownership register naming VAE as a consumer. | Delegation repository with VAE product/adapter owners | Current matrix `INCOMPATIBLE` verdict; RC2 registry/schema; Delegation `CONTRACT_OWNERSHIP_REGISTER.yaml`; VAE FR-037/FR-119/FR-156 | VAE cannot receive accepted, rejected, or alternative-requested proposal disposition through the canonical message path. | Add VAE to the canonical consumer set, regenerate schema/registry/bindings/manifests, then rerun package and VAE matrix/conformance validation. | OPEN - BLOCKING IMPLEMENTATION AUTHORIZATION |

## Local evidence gaps related to cross-repository work

- This directory has no `.git` metadata, so tags, history, and release provenance cannot be inspected.
- `validation/SCHEMA_VALIDATION_REPORT.json` records example passes, but no checked-in executable reproduces those instance validations.
- No local/cloud GPU profile, ComfyUI lock, model set, VAE/LoRA registry, or custom-node lock exists to negotiate with provider/runtime repositories.

These issues must be referenced by the affected ADR and technical specification. A waiver must identify its owner, scope, expiry, provisional assumptions, and the test or migration required when the missing dependency becomes available.
