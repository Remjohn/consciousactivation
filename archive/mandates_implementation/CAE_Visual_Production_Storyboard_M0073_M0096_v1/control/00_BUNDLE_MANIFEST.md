# Bundle Manifest — CAE Visual Production / Storyboard M0073–M0096 v2

This campaign uses a **Component Factory → Assembly → Certification** model. The campaign-supplied Authority Pack is mandatory input and is not assumed to be present in the CAE brownfield repository.

## Required campaign inputs
- `AUTHORITY_PACK/` — frozen Visual Production Update documents and authority manifest
- `mandates/` — executable mandate specifications
- `prompts/` — execution and parallel-routing prompts

## Execution rule
External repositories and isolated components are researched/constructed first. Accepted component bundles are assembled later by M0095. M0096 certifies the integrated product only after live evidence exists.

## M0079 special note
A prior M0079 execution stopped with zero repository changes because the Visual Production Update documents were absent from the supplied brownfield snapshot. That is a valid historical blocker but not completion. M0079 is re-executable under this v2 Authority-Pack model.
