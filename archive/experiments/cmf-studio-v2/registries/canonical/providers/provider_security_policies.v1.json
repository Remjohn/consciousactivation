{
  "registry_id": "registry.providers.provider_security_policies.v1",
  "version": "1.0.0",
  "rules": [
    "Never commit provider secrets.",
    "Do not log full source transcripts or raw API keys.",
    "Real paid provider calls require operator approval or trusted auto-approval policy.",
    "Provider outputs must be downloaded into owned storage and hashed."
  ]
}
