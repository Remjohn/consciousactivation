id: render_qa.ffprobe.compile
version: "1.0.0"
status: active
owner_agent: render_qa_agent
skill_type: deterministic_plus_pydantic
purpose: "Compile render QA receipts from observed metadata without external tool execution."
hard_gates:
  - "Do not call ffprobe."
  - "Do not call ffmpeg."
  - "Do not call Remotion."
  - "Do not call providers."
  - "Do not pass blocking QA failures."
  - "Do not silently downgrade promised motion."
