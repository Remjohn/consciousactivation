id: provider_runtime.job.compile
version: "1.0.0"
status: active
owner_agent: provider_runtime_agent
skill_type: deterministic_plus_pydantic
purpose: "Compile governed Ideogram/Flux provider jobs without calling provider APIs."
hard_gates:
  - "Do not call provider APIs."
  - "Batch requires scene, face plate, and template preview sample approval."
  - "Provider job requires decision log."
  - "Provider output asset ref requires receipt and sha256."
