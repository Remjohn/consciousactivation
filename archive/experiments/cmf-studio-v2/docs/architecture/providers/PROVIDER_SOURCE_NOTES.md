# Provider Source Notes

This bundle keeps provider transports configurable because provider APIs change over time.

Implementation source notes used during planning:

```text
OpenAI image generation docs:
https://developers.openai.com/api/docs/guides/image-generation

Ideogram API docs:
https://developer.ideogram.ai/api-reference/api-reference/generate

Black Forest Labs docs:
https://docs.bfl.ai/

Qwen-Image technical report:
https://arxiv.org/abs/2508.02324
```

Exact endpoint paths, model names, and request fields should be verified against provider docs during live wiring.
