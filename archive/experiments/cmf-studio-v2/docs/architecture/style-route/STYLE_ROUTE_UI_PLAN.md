# Style Route Console V1 UI Plan

The first UI should appear inside SuperVisual Studio as a route inspector.

Panels:

```text
Visual Preproduction Inputs
Route Candidates
Route Inspector
Provider Blueprint / Repairs / Receipts
```

Route forcing must produce a receipt and cannot bypass deterministic hard gates.
