# Carousel Engine Skills V1

Carousel uses shared skills for primitive, visual preproduction, asset intelligence, style route, providers, render, eval, and revision.

Carousel-specific skills live under:

```text
registries/canonical/skills/engines/carousel/
```

The bundle includes 30 Carousel skills, from `carousel.project.create` through `carousel.approval.prepare`.
