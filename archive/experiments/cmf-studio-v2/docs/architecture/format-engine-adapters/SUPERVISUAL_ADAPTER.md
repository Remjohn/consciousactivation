# SuperVisual Format Adapter

The adapter converts a `SuperVisualFormatProgram` into a builder-ready input object.

It keeps:

```text
single source truth
visual hook
edge product
negative space policy
composition grammar
layer stack
style route policy
eval gates
source refs
```

It does not call providers or render.
