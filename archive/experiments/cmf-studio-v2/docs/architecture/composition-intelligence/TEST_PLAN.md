# Test Plan

Tests verify:

```text
brand_context_version_id required
one scene = one concept
cognitive load budget blocks overload
text placement blocks unsafe face collision
real-life cutout requires composition role and source ref
audience proxy requires SFL function
avatar action must serve concept
provider composition plate locks text/layout/avatar
reference edit forbids text rewrite and layout drift
composition commander blocks unresolved cognitive load failures
Format 02 service compiles scene program
```
