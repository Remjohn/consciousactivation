# Test Plan

Tests verify:

```text
character spec is no-lip-sync
PSD requirements reject unsafe paths
face plate approval requires 8 expressions
body layer set requires canonical layers
Stretchy import manifest requires source/layers/bones/sockets
Spine export requires license confirmation
DragonBones export requires JS runtime compatibility
action timeline rejects lip-sync and invalid durations
QA fails missing plates/layers/bones/sockets
rig continuity fails identity drift
Remotion payload is not final render
```
