# Stretchy Studio Manifest

V1 prepares a `StretchyStudioImportManifest`.

It includes:

```text
source PSD ref
layer requirements
skeleton hints
mesh candidates
shape-key candidates
prop sockets
action timeline refs
```

V1 does not call Stretchy Studio directly.
