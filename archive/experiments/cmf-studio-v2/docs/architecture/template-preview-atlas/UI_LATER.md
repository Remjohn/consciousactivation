# UI Later

The operator UI should show:

```text
visual thumbnail
slot labels
sample payload
JSON inspector
template version
approval state
approve / duplicate / edit actions
```

Do not build UI in this bundle.
