# SuperVisual Template Previews

SuperVisual templates represent single-image compositions.

V1 preview rules:

```text
one source truth
one hero object
one power phrase
one negative-space policy
one frame profile
```

The SVG preview should show the hero slot, text slot, and brand mark slot.
