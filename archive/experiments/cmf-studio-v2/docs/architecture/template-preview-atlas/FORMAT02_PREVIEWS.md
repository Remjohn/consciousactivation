# Format 02 Scene Template Previews

Format 02 templates visualize avatar / paper-cut explainer scenes.

V1 preview rules:

```text
one concept per scene
one avatar slot
one hero real-life object slot
one audience proxy slot
low visible text
negative-space area
```

The preview is a static SVG read model, not an animation render.
