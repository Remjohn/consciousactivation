# Format 02 Integration

Format 02 Composition Intelligence decides:

```text
where the avatar goes
what scene role is active
which concept is being taught
which audience proxy mirrors the viewer
```

Avatar Performance Layer decides:

```text
expression plate
body pose
gesture clip
prop attachment
performance timing
audience proxy state
```

Output:

```text
AvatarPerformancePlan
AudienceProxyPerformancePlan
AvatarRenderPayload
```
