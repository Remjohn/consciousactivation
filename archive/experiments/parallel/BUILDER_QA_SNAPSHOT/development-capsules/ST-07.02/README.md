# ST-07.02 Development Capsule

This capsule authorizes no implementation by itself. It packages the bounded `GENERIC_ATOMIC_CONTENT_HARNESS` mode confirmed by BF-AM-007.

The Story compiles one immutable, category-neutral, repository-owned synthetic `AtomicHarnessDefinition` from the already governed Builder Core artifacts. It does not execute the Harness, generate its Development Capsule, register category adapters, implement Format 02, or invoke any external product/runtime.

Read and validate every immutable input, `CAPSULE_MANIFEST.json`, and `IMPLEMENTATION_AUTHORIZATION.yaml` before editing. Use only `ALLOWED_FILE_SCOPE.yaml` after the exact human phrase is supplied.

