# Phase 8 VAE contracts

The `schemas/` directory contains VAE-owned internal development contracts. Delegation schemas
are not copied here; the runtime consumes the exact RC4 release from Program Control and the
Delegation repository.
