# JIT Context Capsule

Required:

- role;
- task;
- immutable object refs;
- minimal rendered context;
- allowed and forbidden actions;
- output schema;
- evaluation contract;
- expiration/disposal behavior.

It exists to prevent monolithic context and authority leakage.
